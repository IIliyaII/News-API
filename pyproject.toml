[project]
name = "news_api"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "logical-ninja", email = "eeleyash@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "beautifulsoup4>=4.15.0",
    "python-dotenv>=1.2.3",
    "requests>=2.34.2",
]

[project.scripts]
news_api = "news_api:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
